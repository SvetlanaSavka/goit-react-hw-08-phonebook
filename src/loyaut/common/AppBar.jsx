import { Navigation } from './Navigation';

export const AppBar = () => {
  return (
    <header>
      <Navigation />
      {/*  <isLoggedIn />
        <UserMenu />
        <AuthNav/>  */}
    </header>
  );
};
